<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ulisboa.tecnico.cmov.hoponcmu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".client.ApplicationContextProvider"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".client.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".client.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".client.ListLocalsActivity" />
        <activity android:name=".client.signupActivity" />
        <activity
            android:name=".client.ReadQuizzAnswersActivity"
            android:label="@string/title_activity_read_quizz_answers"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".client.QuizActivity"></activity>
        <activity android:name=".client.ShareResultsActivity"></activity>

    </application>

</manifest>